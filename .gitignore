node_modules/
dist/
.astro/
.DS_Store
.env
.env.*
!.env.example

# Local source downloads; keep only the directory guidance by default
/data/raw/*
!/data/raw/README.md

# Reproducible intermediate outputs
/data/processed/*
!/data/processed/README.md

# Local Cloudflare state and development secrets (if tooling is introduced)
.wrangler/
.dev.vars
.dev.vars.*

# Local project archives
*.zip
*.7z
*.rar
*.tar
*.gz
# Historical Pulse preview exports are local and unapproved for release; never commit or bundle data in ZIP patches.
/public/data/pulse-of-adelaide/*
!/public/data/pulse-of-adelaide/README.md

# Verified public map reference vectors only; private events stay ignored.
!/public/data/pulse-of-adelaide/geography/
!/public/data/pulse-of-adelaide/geography/**
